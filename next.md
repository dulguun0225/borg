# Next

The work list, not a section of the document. Each entry carries what has already been decided about it, so the next session starts from the decision instead of rediscovering it.

## Still to write

**Adoption of an existing codebase is never designed.** The single mention is in [_What a diff cannot see_](how-humans-do-it/07-contracts.md#what-a-diff-cannot-see): an interface adopted from outside the factory arrives without declared meaning, and retrofitting the name is itself a breaking change. Everything else assumes a factory that has always been there — contracts declared with modes, a design system chosen, an authorship prior with history behind it, a production deploy record to be the current release, a control standing for the first comparison. A first run has none of them, and the score starting wide covers only the authorship half. It may be [_Deferred, but not designed out_](deferred.md) material rather than a section: the question to settle first is whether adoption is designed now or named as sequenced away, with what it would cost to retrofit stated either way.
