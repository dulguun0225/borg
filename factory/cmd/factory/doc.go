// Command factory is the crude interface M1 defers the four surfaces with:
// one binary on a terminal, until M7 builds Work, Ops, Factory, and People.
//
// Two subcommands. "run" walks the whole path once — intake, the interview,
// the cut, the spec and implementation stages, the build, the criteria in
// force checked in both directions, the Merge to master gate with a human
// deciding, the fast-forward, the release, and a straight deploy — stopping
// with the first error. "walk <deploy-id>" follows the links from an existing deploy
// record back to its intent, which is the direction the milestone is
// demonstrated in: every step a stored field, none reconstructed.
//
// Who may write what: nothing of its own. Every record the run causes to
// exist is written by the package that owns it; this command composes the
// writers and holds no table, and every read goes through the owning
// package's readers.
//
// What defines it: roadmap M1 — "the interface the human decides through is
// whatever is cheapest — the four surfaces come at M7, and a crude interface
// until then is what deferring them costs" —
// ../../../roadmap.md#m1--one-change-ships.
package main
