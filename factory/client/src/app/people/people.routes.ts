import { Routes } from '@angular/router';
import { PeopleScreen } from './people';

// Every address of the People screen: the declaration alone, and nothing
// under it.
export const peopleRoutes: Routes = [{ path: '', component: PeopleScreen }];
