# Intake

What arrives is an **intent**: a request from an owner (1), the end-user [_reports_](02-reports.md) a grouper turned into one (4, 5), or something the factory found itself, raised by a **detector**. Four detectors read what runs: the [health monitor](../../08-operations/01-the-health-monitor.md) still running after its [window](../../08-operations/02-the-analysis-window.md) closed, a [consumer](../../07-contracts/01-two-versioned-things.md) breaking in its own error rate, an [_advisory_](03-advisories.md) naming a package version a shipped [build](../../05-environments/01-records-and-one-long-lived-branch.md) resolved, and a [service level objective](../../08-operations/05-service-level-objectives.md) whose error budget is exhausted or being spent at a rate that exhausts it inside its period. The fifth reads the records: a permanent [constraint](01-constraints-and-the-design-system.md) in force with shipped services inside its reach and no intent on its record. The sixth reads a criterion's own record: an outcome history crossing its [unreliability bound](../../03-gates/07-what-particular-gates-decide/02-spec/02-in-force-and-withdrawal.md). A seventh, the pass over the [deprecation list](../../07-contracts/08-deprecation.md), reads the contracts in force and a brownout's window; [_Deprecation_](../../07-contracts/08-deprecation.md) defines it. One record, three sources, and one writer — intake writes every intent, and the three sources are three callers of one entrance rather than three components writing the same record. What it writes at that event is the source, the actor (seam 1 of [_Deferred, but not designed out_](../../../deferred.md)), whatever constraint arrived with the request and binds only what is decomposed from it, and, for an intent the factory raised, a link to the evidence that raised it. A [revert](../../06-releases/06-rollback.md) a human asks for at Ops carries the same link, to the release it undoes. An intent arrives unrefined, and nothing is judged on the way in, because judging it is what [the interview](../02-the-interview.md) is for.

For an intent a detector raised, intake also writes its [tier](../02-the-interview.md) at arrival, from the constraint or the detector that raised it; for the other two sources it is written later, at the interview's last round, and not here. Nothing here is a judgment either way: the tier arrives authored, and intake writes it only because intake is the intent's one writer.

It also writes the [project](../../11-screens/01-work-ops-factory-people.md) the request came in under, where a source supplies one — the project an owner raised it under, the project of the [service](../03-decomposition/README.md) a grouped report came from, or the project named in a detector's evidence. That field has one job: it is where [_Decomposition_](../03-decomposition/README.md) places a service the work creates, there being no existing service to reach a project through. It is not a claim about where the work is, because a [contract migration](../../07-contracts/02-no-single-item-may-break-a-contract.md)'s decomposed items may be in two projects, and it is not what scopes the constraints [_The interview_](../02-the-interview.md) reads. Where no source supplies one the field stays unwritten, and intake fills it once if decomposition has to place a created service and nothing else answers; it is never rewritten, so an approval keeps pointing at what was approved. Putting the project on the intent and not on the item is not a contradiction of [_What an item names_](../03-decomposition/02-what-an-item-names.md): an item names a service and reaches a project through it, and an intent names no service.

One writer costs a dependency edge from the far end of [the pipeline](../../01-one-pipeline.md) back to its start: [operations](../../08-operations/README.md) calls intake to raise what it found. What several writers would cost is one record's rules — the actor always populated, [the chain](../../../deferred.md), the links — implemented once per writer and drifting apart, on the record every other record links back to.

The intent is what [the graph](../../../what-the-factory-does/01-tight-integration.md) joins on. Every item links back to it, which is why one request producing four items in four services needs no record type of its own — the same point [_Work that spans services_](../../07-contracts/10-work-that-spans-services.md) makes from the other end.

The factory's own source repeats where the other two do not: a health monitor still running is still running tomorrow, and a consumer breaking in its own error rate goes on breaking. So a detector raises an intent for a condition and not for an observation — before raising one it looks for an intent on the same evidence that has not finished, and attaches to that one instead. The evidence is what keys it: this service, this consumer, this [contract](../../07-contracts/01-two-versioned-things.md), this [release](../../06-releases/02-the-release-record.md), this constraint, this objective's period — and for [_Deprecation_](../../07-contracts/08-deprecation.md)'s raises, the element, two marked elements of one contract being two removals. Keyed too finely it raises an intent per observation; too coarsely it attaches two problems to one intent, which decomposition then has to split.

| Subsection | What it settles |
|---|---|
| [Constraints and the design system](01-constraints-and-the-design-system.md) | What a constraint binds, its kind, and how a design system fits it |
| [Reports](02-reports.md) | How end-user reports become one intent per group |
| [Advisories](03-advisories.md) | How an advisory matching a shipped build raises an intent |
